#include <bits/stdc++.h>
using namespace std;
int main(){
    list<int> ls;
    ls.push_back(2);    
    ls.push_front(4);
    //emplace back and emplace front also
    //rest fn same as vector
    //Deque container also the same
}    